fix(auth): revert to proxy.ts for Next.js 16.2.4 Edge compatibility

- A versão experimental Turbopack do Next.js 16.2 exige que o arquivo seja nomeado `proxy.ts` em vez de `middleware.ts`.
- Renomear o arquivo com o servidor de desenvolvimento rodando causava travamentos 500. A reversão estabiliza o servidor.
