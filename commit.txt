fix(data): bypass next.js HMR and worker cache by reading directly from disk

- Substituição completa do padrão in-memory (Map) no ChildRepository por leitura/escrita atômica em disco a cada requisição.
- Resolve o bug de sincronia onde o cache de desenvolvimento do Next.js (HMR) revertia visualmente o status "Revisado" após um router.refresh() renderizar um worker com memória desatualizada.
