fix(data): persist review status to seed.json file

- Adicionada a gravação em disco (fs.writeFileSync) no método update() do ChildRepository.
- Isso garante que, mesmo não utilizando um banco de dados real (ORM), o estado das crianças não seja perdido caso o servidor de desenvolvimento reinicie ou o contêiner Docker sofra reload.
